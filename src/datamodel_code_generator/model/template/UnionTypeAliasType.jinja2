{%- if description %}
# {{ description | replace('\n', '\n# ') }}
{%- endif %}
{%- if fields|length > 1 %}
{{ class_name }} = TypeAliasType("{{ class_name }}", Union[
{%- for field in fields %}
    '{{ field.name }}',
{%- endfor %}
]){% else %}
{{ class_name }} = TypeAliasType("{{ class_name }}", {{ fields[0].name }}){% endif %}
