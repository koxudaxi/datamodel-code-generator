"""Native Python 3.14 annotation evaluation without a future import, matching the requested target."""


class Child:
    """Resolve a self reference through Python 3.14 deferred annotations."""

    sibling: Child
