# generated by datamodel-codegen:
#   filename:  root_pattern_properties_object_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

import re
from typing import Any

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    RootModel,
    conint,
    constr,
    model_validator,
)


class RootPatternPropertiesObjectValidators1(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    id: conint(ge=1)
    enabled: bool


class RootPatternPropertiesObjectValidators(
    RootModel[
        dict[constr(pattern=r'^item-'), RootPatternPropertiesObjectValidators1]
        | dict[str, Any]
    ]
):
    root: (
        dict[constr(pattern=r'^item-'), RootPatternPropertiesObjectValidators1]
        | dict[str, Any]
    ) = Field(..., title='root pattern properties object validators')

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(mode='python', exclude_unset=True)
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        if isinstance(self.root, dict):
            for extra_key, extra_value in self.root.items():
                extra_value = json_schema_runtime_value(extra_value)
                matched_pattern = False
                if re.search('^item-', extra_key):
                    matched_pattern = True
                    if not (
                        (isinstance(extra_value, dict))
                        and (
                            (
                                not isinstance(extra_value, dict)
                                or {'id', 'enabled'}.issubset(extra_value)
                            )
                        )
                        and (
                            (
                                not isinstance(extra_value, dict)
                                or 'id' not in extra_value
                                or (
                                    lambda extra_value_property_0: (
                                        isinstance(extra_value_property_0, int)
                                        and not isinstance(extra_value_property_0, bool)
                                    )
                                    and (
                                        isinstance(extra_value_property_0, (int, float))
                                        and not isinstance(extra_value_property_0, bool)
                                        and extra_value_property_0 >= 1
                                    )
                                )(extra_value['id'])
                            )
                        )
                        and (
                            (
                                not isinstance(extra_value, dict)
                                or 'enabled' not in extra_value
                                or (
                                    lambda extra_value_property_1: isinstance(
                                        extra_value_property_1, bool
                                    )
                                )(extra_value['enabled'])
                            )
                        )
                        and (
                            (
                                not isinstance(extra_value, dict)
                                or all(
                                    (extra_key in {'id', 'enabled'})
                                    for extra_key in extra_value
                                )
                            )
                        )
                    ):
                        raise ValueError(
                            'root property '
                            + extra_key
                            + ' does not match pattern schema'
                        )
                if re.search('^blocked', extra_key):
                    matched_pattern = True
                    raise ValueError(
                        'root property ' + extra_key + ' matches forbidden pattern'
                    )
                if not matched_pattern:
                    raise ValueError(
                        'root property '
                        + extra_key
                        + ' does not match any allowed pattern'
                    )
        return self
