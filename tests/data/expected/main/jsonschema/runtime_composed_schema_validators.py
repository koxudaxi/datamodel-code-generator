# generated by datamodel-codegen:
#   filename:  runtime_composed_schema_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

import re
from typing import Any

from pydantic import BaseModel, ConfigDict, model_validator


class RuntimeComposedSchemaValidators(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    gate: bool | None = None
    values: list[Any] | None = None
    name: str | None = None

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        model_data = {
            field_name: json_schema_runtime_value(getattr(self, field_name))
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )
        if 'gate' in provided_keys:
            missing = {'values'} - provided_keys
            if missing:
                raise ValueError(
                    'gate' + ' requires properties: ' + ', '.join(sorted(missing))
                )
        if 'gate' in provided_keys and not (
            ((not isinstance(model_data, dict) or {'values'}.issubset(model_data)))
            and (
                (
                    not isinstance(model_data, dict)
                    or 'values' not in model_data
                    or (
                        lambda model_data_property_0: (
                            isinstance(model_data_property_0, list)
                        )
                        and (
                            (
                                not isinstance(model_data_property_0, list)
                                or all(
                                    (
                                        lambda extra_item: (
                                            (
                                                (
                                                    isinstance(extra_item, int)
                                                    and not isinstance(extra_item, bool)
                                                )
                                                and (
                                                    isinstance(extra_item, (int, float))
                                                    and not isinstance(extra_item, bool)
                                                    and extra_item >= 2
                                                )
                                            )
                                            or (
                                                (isinstance(extra_item, str))
                                                and (
                                                    isinstance(extra_item, str)
                                                    and re.search('^v-', extra_item)
                                                    is not None
                                                )
                                            )
                                        )
                                        and (not (extra_item == 'v-bad'))
                                    )(extra_item)
                                    for extra_item in model_data_property_0
                                )
                            )
                        )
                    )(model_data['values'])
                )
            )
        ):
            raise ValueError('gate' + ' requires dependent schema to match')

        for extra_key, extra_value in extra_values.items():
            if re.search('^x-', extra_key) is not None:
                continue
            if not (
                (
                    (isinstance(extra_value, str))
                    and (
                        isinstance(extra_value, str)
                        and re.search('^extra-', extra_value) is not None
                    )
                )
                or (
                    (isinstance(extra_value, int) and not isinstance(extra_value, bool))
                    and (
                        isinstance(extra_value, (int, float))
                        and not isinstance(extra_value, bool)
                        and extra_value >= 100
                    )
                )
            ):
                raise ValueError(
                    'additional property ' + extra_key + ' does not match schema'
                )

        for extra_key, extra_value in extra_values.items():
            extra_value = json_schema_runtime_value(extra_value)
            matched_pattern = False
            if re.search('^x-', extra_key):
                matched_pattern = True
                if not (
                    sum(
                        1
                        for matched in (
                            (
                                isinstance(extra_value, int)
                                and not isinstance(extra_value, bool)
                            )
                            and (
                                isinstance(extra_value, (int, float))
                                and not isinstance(extra_value, bool)
                                and extra_value >= 1
                            ),
                            (isinstance(extra_value, str))
                            and (
                                isinstance(extra_value, str)
                                and re.search('^x-', extra_value) is not None
                            ),
                        )
                        if matched
                    )
                    == 1
                ):
                    raise ValueError(
                        'additional property '
                        + extra_key
                        + ' does not match pattern schema'
                    )
        return self
