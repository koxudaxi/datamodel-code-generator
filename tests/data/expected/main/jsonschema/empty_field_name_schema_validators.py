# generated by datamodel-codegen:
#   filename:  empty_field_name_schema_validators.json

from __future__ import annotations

import re
from typing import Annotated, Any, ClassVar

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    RootModel,
    TypeAdapter,
    model_validator,
)


class _JsonSchemaRuntimeValidationBase(BaseModel):
    __json_schema_pattern_properties__: ClassVar[tuple[Any, ...]] = ()

    @model_validator(mode='before')
    @classmethod
    def _validate_json_schema_runtime_rules(cls, data: Any) -> Any:
        data = cls._validate_json_schema_pattern_properties(data)
        return data

    @classmethod
    def _validate_json_schema_pattern_properties(cls, data: Any) -> Any:
        if not isinstance(data, dict):
            return data
        values = data
        for rule in cls.__json_schema_pattern_properties__:
            pattern_adapters = None
            additional_adapter = None
            for key, value in data.items():
                if key in rule['declared_properties']:
                    continue
                if any(
                    re.search(pattern, key) for pattern in rule['rejected_patterns']
                ):
                    raise ValueError(
                        f'Property {key!r} is not allowed by patternProperties'
                    )
                matched = False
                for index, (pattern, value_type) in enumerate(rule['pattern_properties']):
                    if not re.search(pattern, key):
                        continue
                    matched = True
                    if pattern_adapters is None:
                        pattern_adapters = {}
                    if (adapter := pattern_adapters.get(index)) is None:
                        adapter = TypeAdapter(value_type)
                        pattern_adapters[index] = adapter
                    value = adapter.validate_python(value)
                if matched:
                    if values is data:
                        values = dict(data)
                    values[key] = value
                    continue
                if rule['additional_property_type'] is not None:
                    if additional_adapter is None:
                        additional_adapter = TypeAdapter(rule['additional_property_type'])
                    if values is data:
                        values = dict(data)
                    values[key] = additional_adapter.validate_python(value)
                    continue
                if not rule['allow_unmatched']:
                    raise ValueError(f'Unexpected property {key!r}')
        return values


class EmptyBase(BaseModel):
    field_: Annotated[str | None, Field(alias='')] = None


class DirectEmptyPattern(_JsonSchemaRuntimeValidationBase):
    model_config = ConfigDict(
        extra='allow',
    )

    __json_schema_pattern_properties__: ClassVar[tuple[Any, ...]] = (
        {
            'declared_properties': ('',),
            'rejected_patterns': (),
            'pattern_properties': (('^x_[0-9]+$', int),),
            'additional_property_type': None,
            'allow_unmatched': False,
        },
    )

    field_: Annotated[str | None, Field(alias='')] = None


class InheritedEmptyPattern(_JsonSchemaRuntimeValidationBase, EmptyBase):
    model_config = ConfigDict(
        extra='allow',
    )

    __json_schema_pattern_properties__: ClassVar[tuple[Any, ...]] = (
        {
            'declared_properties': ('',),
            'rejected_patterns': (),
            'pattern_properties': (('^x_[0-9]+$', int),),
            'additional_property_type': None,
            'allow_unmatched': False,
        },
    )


class Model(RootModel[DirectEmptyPattern]):
    root: DirectEmptyPattern
