# generated by datamodel-codegen:
#   filename:  root_array_unique_items_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import Field, RootModel, model_validator


class RootArrayUniqueItemsValidators(RootModel[list[int | bool]]):
    root: list[int | bool] = Field(..., title='root array unique items validators')

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_unique_key(value):
            value = json_schema_runtime_value(value)
            if isinstance(value, bool) or value is None or isinstance(value, str):
                return (type(value).__name__, value)
            if isinstance(value, (int, float)):
                return ('number', value)
            if isinstance(value, list):
                return ('array', tuple(json_schema_unique_key(item) for item in value))
            if isinstance(value, dict):
                return (
                    'object',
                    tuple(
                        sorted(
                            (key, json_schema_unique_key(item))
                            for key, item in value.items()
                        )
                    ),
                )
            return (type(value).__name__, value)

        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        if self.root is not None and not (
            (
                (
                    not isinstance(self.root, list)
                    or all(
                        (
                            lambda extra_item: (
                                isinstance(extra_item, int)
                                and not isinstance(extra_item, bool)
                            )
                            or (isinstance(extra_item, bool))
                        )(extra_item)
                        for extra_item in self.root
                    )
                )
            )
            and (
                (
                    not isinstance(self.root, list)
                    or len(self.root)
                    == len(
                        {
                            json_schema_unique_key(unique_item)
                            for unique_item in self.root
                        }
                    )
                )
            )
        ):
            raise ValueError('root' + ' does not match array schema')
        return self
