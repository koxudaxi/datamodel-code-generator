# generated by datamodel-codegen:
#   filename:  array_unevaluated_items_contains_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import BaseModel, conint, model_validator


class ArrayUnevaluatedItemsContainsValidators(BaseModel):
    values: list[str | conint(ge=1)]

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        if self.values is not None:
            values_match_count = sum(
                1
                for item in self.values
                if (
                    lambda item: (isinstance(item, int) and not isinstance(item, bool))
                    and (
                        isinstance(item, (int, float))
                        and not isinstance(item, bool)
                        and item >= 1
                    )
                )(json_schema_runtime_value(item))
            )
            if values_match_count < 1:
                raise ValueError('values' + ' must contain at least 1 matching item(s)')

        if self.values is not None and not (
            (
                (
                    not isinstance(self.values, list)
                    or len(self.values) <= 0
                    or (lambda extra_item: isinstance(extra_item, str))(self.values[0])
                )
            )
            and (
                (
                    not isinstance(self.values, list)
                    or all(
                        (
                            lambda extra_item: (
                                isinstance(extra_item, int)
                                and not isinstance(extra_item, bool)
                            )
                            and (
                                isinstance(extra_item, (int, float))
                                and not isinstance(extra_item, bool)
                                and extra_item >= 1
                            )
                        )(extra_item)
                        for extra_item in self.values[1:]
                    )
                )
            )
        ):
            raise ValueError('values' + ' does not match array schema')
        return self
