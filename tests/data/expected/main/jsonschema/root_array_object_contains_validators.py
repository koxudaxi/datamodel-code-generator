# generated by datamodel-codegen:
#   filename:  root_array_object_contains_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, RootModel, model_validator


class RootArrayObjectContainsValidator(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    kind: str
    score: int


class RootArrayObjectContainsValidators(
    RootModel[list[RootArrayObjectContainsValidator]]
):
    root: list[RootArrayObjectContainsValidator] = Field(
        ..., title='root array object contains validators'
    )

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        if self.root is not None:
            root_match_count = sum(
                1
                for item in self.root
                if (
                    lambda item: (isinstance(item, dict))
                    and (
                        (not isinstance(item, dict) or {'kind', 'score'}.issubset(item))
                    )
                    and (
                        (
                            not isinstance(item, dict)
                            or 'kind' not in item
                            or (lambda item_property_0: item_property_0 == 'hit')(
                                item['kind']
                            )
                        )
                    )
                    and (
                        (
                            not isinstance(item, dict)
                            or 'score' not in item
                            or (
                                lambda item_property_1: (
                                    isinstance(item_property_1, int)
                                    and not isinstance(item_property_1, bool)
                                )
                                and (
                                    isinstance(item_property_1, (int, float))
                                    and not isinstance(item_property_1, bool)
                                    and item_property_1 >= 10
                                )
                            )(item['score'])
                        )
                    )
                    and (
                        (
                            not isinstance(item, dict)
                            or all(
                                (extra_key in {'kind', 'score'}) for extra_key in item
                            )
                        )
                    )
                )(json_schema_runtime_value(item))
            )
            if root_match_count < 2:
                raise ValueError('root' + ' must contain at least 2 matching item(s)')
            if root_match_count > 2:
                raise ValueError('root' + ' must contain at most 2 matching item(s)')
        return self
