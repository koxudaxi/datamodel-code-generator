# generated by datamodel-codegen:
#   filename:  x_python_type_target_newer_symbols.json

from collections.abc import Buffer
from datetime import timezone
from decimal import DecimalException, DecimalTuple
from enum import ReprEnum, auto, member, nonmember, property, verify
from re import RegexFlag
from typing import ReadOnly, TypedDict, TypeIs
from uuid import SafeUUID


class TargetNewerSymbols(TypedDict):
    typing_symbols: tuple[ReadOnly[str], TypeIs[str], Buffer]
    numeric_symbols: tuple[DecimalException, DecimalTuple, SafeUUID]
    runtime_symbols: tuple[timezone, ReprEnum, RegexFlag]
    enum_helpers: tuple[auto, property, verify, member, nonmember]
