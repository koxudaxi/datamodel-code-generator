# generated by datamodel-codegen:
#   filename:  root.json

from __future__ import annotations

from collections.abc import Iterator, Sequence
from typing import SupportsIndex, overload

from pydantic import Field, RootModel

from . import items


class Items(RootModel[Sequence[items.Item]], Sequence[items.Item]):
    root: Sequence[items.Item] = Field(..., title='Items')

    def __iter__(self) -> Iterator[items.Item]:
        return iter(self.root)

    @overload
    def __getitem__(self, index: SupportsIndex) -> items.Item:
        pass

    @overload
    def __getitem__(self, index: slice) -> Sequence[items.Item]:
        pass

    def __getitem__(
        self, index: SupportsIndex | slice
    ) -> items.Item | Sequence[items.Item]:
        return self.root[index]

    def __len__(self) -> int:
        return len(self.root)
