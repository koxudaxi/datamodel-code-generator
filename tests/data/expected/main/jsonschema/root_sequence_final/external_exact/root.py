# generated by datamodel-codegen:
#   filename:  root.json

from __future__ import annotations

from collections.abc import Iterator, Sequence
from typing import SupportsIndex, overload

from pydantic import Field, RootModel

from .items import Item


class Items(RootModel[list[Item]], Sequence[Item]):
    root: list[Item] = Field(..., title='Items')

    def __iter__(self) -> Iterator[Item]:
        return iter(self.root)

    @overload
    def __getitem__(self, index: SupportsIndex) -> Item:
        pass

    @overload
    def __getitem__(self, index: slice) -> list[Item]:
        pass

    def __getitem__(self, index: SupportsIndex | slice) -> Item | list[Item]:
        return self.root[index]

    def __len__(self) -> int:
        return len(self.root)
