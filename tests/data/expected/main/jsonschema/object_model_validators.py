# generated by datamodel-codegen:
#   filename:  object_model_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, model_validator


class ObjectModelValidators(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    credit_card: float | None = Field(None, alias='credit-card')
    billing_address: str | None = Field(None, alias='billing-address')
    memo: str | None = None
    note: str | None = None

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(mode='python')
            return value

        field_count = len(self.model_fields_set)
        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        field_count += len(extra_values)
        if field_count < 2:
            raise ValueError('Expected at least 2 properties')
        if field_count > 3:
            raise ValueError('Expected at most 3 properties')

        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        field_original_names = {
            'credit_card': 'credit-card',
            'billing_address': 'billing-address',
        }
        model_data = {
            field_original_names.get(field_name, field_name): json_schema_runtime_value(
                getattr(self, field_name)
            )
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )
        if 'credit_card' in provided_keys:
            missing = {'billing_address'} - provided_keys
            if missing:
                raise ValueError(
                    'credit_card'
                    + ' requires properties: '
                    + ', '.join(sorted(missing))
                )
        if 'note' in provided_keys:
            missing = {'memo'} - provided_keys
            if missing:
                raise ValueError(
                    'note' + ' requires properties: ' + ', '.join(sorted(missing))
                )
        if 'memo' in provided_keys:
            missing = {'note'} - provided_keys
            if missing:
                raise ValueError(
                    'memo' + ' requires properties: ' + ', '.join(sorted(missing))
                )
        if 'memo' in provided_keys and not (
            (not isinstance(model_data, dict) or {'note'}.issubset(model_data))
        ):
            raise ValueError('memo' + ' requires dependent schema to match')
        return self
