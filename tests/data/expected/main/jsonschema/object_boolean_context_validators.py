# generated by datamodel-codegen:
#   filename:  object_boolean_context_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, model_validator


class ObjectBooleanContextValidators(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    default_config: str | None = Field(None, alias='default-config')
    legacy_mode: bool | None = Field(None, alias='legacy-mode')
    activation_code: str | None = Field(None, alias='activation-code')

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(mode='python')
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        field_original_names = {
            'default_config': 'default-config',
            'legacy_mode': 'legacy-mode',
            'activation_code': 'activation-code',
        }
        model_data = {
            field_original_names.get(field_name, field_name): json_schema_runtime_value(
                getattr(self, field_name)
            )
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )
        if 'legacy_mode' in provided_keys and not (False):
            raise ValueError('legacy_mode' + ' requires dependent schema to match')

        if True:
            if not (
                (
                    (
                        not isinstance(model_data, dict)
                        or {'default-config'}.issubset(model_data)
                    )
                )
                and (
                    (
                        not isinstance(model_data, dict)
                        or 'default-config' not in model_data
                        or (
                            lambda model_data_property_0: (
                                isinstance(model_data_property_0, str)
                            )
                            and (
                                isinstance(model_data_property_0, str)
                                and len(model_data_property_0) >= 3
                            )
                        )(model_data['default-config'])
                    )
                )
            ):
                raise ValueError('then schema must match')
        return self
