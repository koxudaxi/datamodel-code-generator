# generated by datamodel-codegen:
#   filename:  property_names_explicit_properties.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

import re

from pydantic import BaseModel, ConfigDict, model_validator


class PropertyNamesExplicitProperties(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    good: str | None = None
    bad: str | None = None

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        model_data = {
            field_name: json_schema_runtime_value(getattr(self, field_name))
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )
        for property_key in model_data:
            if not (
                (
                    not isinstance(property_key, str)
                    or re.search('^(good|x-[a-z]+)$', property_key) is not None
                )
            ):
                raise ValueError(
                    'property name ' + property_key + ' does not match schema'
                )

        for extra_key, extra_value in extra_values.items():
            if not (isinstance(extra_value, int) and not isinstance(extra_value, bool)):
                raise ValueError(
                    'additional property ' + extra_key + ' does not match schema'
                )
        return self
