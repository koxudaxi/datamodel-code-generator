# generated by datamodel-codegen:
#   filename:  property_raw_not_object_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from pydantic import BaseModel, model_validator


class Payload(BaseModel):
    a: int | None = None

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        model_data = {
            field_name: json_schema_runtime_value(getattr(self, field_name))
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )

        if not isinstance(model_data, dict) or {'b'}.issubset(model_data):
            raise ValueError('not schema must not match')
        return self


class PropertyRawNotObjectValidators(BaseModel):
    payload: Payload

    @model_validator(mode='before')
    @classmethod
    def validate_json_schema_input_constraints(cls, data):
        if isinstance(data, dict):
            if 'payload' in data:
                payload_raw_value = data['payload']
                if not (
                    (isinstance(payload_raw_value, dict))
                    and (
                        (
                            not isinstance(payload_raw_value, dict)
                            or 'a' not in payload_raw_value
                            or (
                                lambda payload_raw_value_property_0: isinstance(
                                    payload_raw_value_property_0, int
                                )
                                and not isinstance(payload_raw_value_property_0, bool)
                            )(payload_raw_value['a'])
                        )
                    )
                    and (
                        not (
                            (
                                not isinstance(payload_raw_value, dict)
                                or {'b'}.issubset(payload_raw_value)
                            )
                        )
                    )
                ):
                    raise ValueError('payload' + ' does not match schema')
        return data
