# generated by datamodel-codegen:
#   filename:  additional_properties_type_specific_constraints.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

import re

from pydantic import BaseModel, ConfigDict, model_validator


class AdditionalPropertiesTypeSpecificConstraints(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    name: str

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        extra_values = getattr(self, '__pydantic_extra__', None) or {}

        for extra_key, extra_value in extra_values.items():
            if not (
                (
                    (isinstance(extra_value, str))
                    or (
                        isinstance(extra_value, (int, float))
                        and not isinstance(extra_value, bool)
                    )
                    or (isinstance(extra_value, list))
                    or (isinstance(extra_value, bool))
                    or (isinstance(extra_value, dict))
                )
                and (
                    ((not isinstance(extra_value, str) or len(extra_value) <= 3))
                    and (
                        (
                            not isinstance(extra_value, str)
                            or re.search('^[A-Z]+$', extra_value) is not None
                        )
                    )
                    and (
                        (
                            not (
                                isinstance(extra_value, (int, float))
                                and not isinstance(extra_value, bool)
                            )
                            or extra_value >= 10
                        )
                    )
                    and (
                        (
                            not (
                                isinstance(extra_value, (int, float))
                                and not isinstance(extra_value, bool)
                            )
                            or (extra_value / 2.0).is_integer()
                        )
                    )
                    and ((not isinstance(extra_value, list) or len(extra_value) <= 2))
                )
            ):
                raise ValueError(
                    'additional property ' + extra_key + ' does not match schema'
                )
        return self
