# generated by datamodel-codegen:
#   filename:  root_object_context_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from typing import Any

from pydantic import RootModel, model_validator


class RootObjectContextValidators(RootModel[dict[str, Any]]):
    root: dict[str, Any]

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        root_value = json_schema_runtime_value(self.root)
        if not (
            (isinstance(root_value, dict))
            and (
                (
                    not isinstance(root_value, dict)
                    or 'credit' not in root_value
                    or {'billing'}.issubset(root_value)
                )
            )
            and (
                (
                    not (
                        (
                            (
                                not isinstance(root_value, dict)
                                or {'kind'}.issubset(root_value)
                            )
                        )
                        and (
                            (
                                not isinstance(root_value, dict)
                                or 'kind' not in root_value
                                or (
                                    lambda root_value_property_0: root_value_property_0
                                    == 'business'
                                )(root_value['kind'])
                            )
                        )
                    )
                    or (
                        (
                            not isinstance(root_value, dict)
                            or {'tax_id'}.issubset(root_value)
                        )
                    )
                )
            )
            and (
                not (
                    (
                        not isinstance(root_value, dict)
                        or {'forbidden'}.issubset(root_value)
                    )
                )
            )
        ):
            raise ValueError('root object does not match schema')
        return self
