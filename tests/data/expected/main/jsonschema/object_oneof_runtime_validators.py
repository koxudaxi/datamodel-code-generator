# generated by datamodel-codegen:
#   filename:  object_oneof_runtime_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, model_validator


class ObjectOneofRuntimeValidators(BaseModel):
    anything_except_string: Any | str
    number_not_integer: int | float

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(
                    mode='python', by_alias=True, exclude_unset=True
                )
            if isinstance(value, (list, tuple)):
                return [json_schema_runtime_value(item) for item in value]
            if isinstance(value, dict):
                return {
                    key: json_schema_runtime_value(item) for key, item in value.items()
                }
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}
        provided_keys = set(self.model_fields_set)
        provided_keys.update(extra_values)
        model_data = {
            field_name: json_schema_runtime_value(getattr(self, field_name))
            for field_name in self.model_fields_set
        }
        model_data.update(
            {
                key: json_schema_runtime_value(value)
                for key, value in extra_values.items()
            }
        )
        if 'anything_except_string' in provided_keys:
            anything_except_string_value = json_schema_runtime_value(
                self.anything_except_string
            )
            if not (
                sum(
                    1
                    for matched in (True, isinstance(anything_except_string_value, str))
                    if matched
                )
                == 1
            ):
                raise ValueError(
                    'anything_except_string' + ' does not match oneOf schema'
                )
        if 'number_not_integer' in provided_keys:
            number_not_integer_value = json_schema_runtime_value(
                self.number_not_integer
            )
            if not (
                sum(
                    1
                    for matched in (
                        isinstance(number_not_integer_value, int)
                        and not isinstance(number_not_integer_value, bool),
                        isinstance(number_not_integer_value, (int, float))
                        and not isinstance(number_not_integer_value, bool),
                    )
                    if matched
                )
                == 1
            ):
                raise ValueError('number_not_integer' + ' does not match oneOf schema')
        return self
