# generated by datamodel-codegen:
#   filename:  factory.json

from __future__ import annotations

from dataclasses import dataclass
from typing import Literal, Optional


def build_Record():
    annotations = {
        'items': Optional[dict[str, list[int]]],
        'Optional': Optional[str],
        'choice': Optional[Literal['Optional', 'list', 'Field']],
    }

    @dataclass
    class Record:
        __annotations__ = annotations
        items = None
        Optional = 'Optional Field list — 名前'
        choice = 'Optional'

    return Record


Record = build_Record()
