# generated by datamodel-codegen:
#   filename:  pattern_properties_closed_object_validators.json
#   timestamp: 2019-07-26T00:00:00+00:00

from __future__ import annotations

import re

from pydantic import BaseModel, ConfigDict, model_validator


class PatternPropertiesClosedObjectValidators(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    id: str

    @model_validator(mode='after')
    def validate_json_schema_constraints(self):
        def json_schema_runtime_value(value):
            if hasattr(value, 'model_dump'):
                return value.model_dump(mode='python')
            return value

        extra_values = getattr(self, '__pydantic_extra__', None) or {}

        for extra_key, extra_value in extra_values.items():
            extra_value = json_schema_runtime_value(extra_value)
            matched_pattern = False
            if re.search('^s_', extra_key):
                matched_pattern = True
                if not (
                    (isinstance(extra_value, str))
                    and (isinstance(extra_value, str) and len(extra_value) >= 2)
                ):
                    raise ValueError(
                        'additional property '
                        + extra_key
                        + ' does not match pattern schema'
                    )
            if re.search('^i_', extra_key):
                matched_pattern = True
                if not (
                    (isinstance(extra_value, int) and not isinstance(extra_value, bool))
                    and (
                        isinstance(extra_value, (int, float))
                        and not isinstance(extra_value, bool)
                        and extra_value >= 1
                    )
                ):
                    raise ValueError(
                        'additional property '
                        + extra_key
                        + ' does not match pattern schema'
                    )
            if re.search('^blocked_', extra_key):
                matched_pattern = True
                raise ValueError(
                    'additional property ' + extra_key + ' matches forbidden pattern'
                )
            if not matched_pattern:
                raise ValueError(
                    'additional property '
                    + extra_key
                    + ' does not match any allowed pattern'
                )
        return self
