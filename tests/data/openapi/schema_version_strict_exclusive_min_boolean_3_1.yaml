openapi: "3.1.0"
info:
  title: ExclusiveMinBooleanTest
  version: "1.0"
paths: {}
components:
  schemas:
    Root:
      type: object
      properties:
        value:
          $ref: "#/components/schemas/BoundedInt"
    BoundedInt:
      type: integer
      minimum: 0
      exclusiveMinimum: true
