openapi: '3.0.2'
info:
  version: 1.0.0
  title: Inline Const Test
components:
  schemas:
    Model:
      type: object
      properties:
        type:
          type: string
          const: "constant_value"
        name:
          type: string
      required:
        - type
        - name
