openapi: 3.0.0
info:
  title: Unrelated invalid dotted model
  version: 1.0.0
paths: {}
components:
  schemas:
    A.1.Unique:
      type: object
      properties:
        value:
          type: string
    ValidA.Foo:
      type: object
      properties:
        left:
          type: string
    ValidB.Foo:
      type: object
      properties:
        right:
          type: integer
