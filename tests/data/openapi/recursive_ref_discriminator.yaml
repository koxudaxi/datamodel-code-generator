openapi: "3.0.3"
info:
  title: Test
  version: "1.0"
paths: {}
components:
  schemas:
    ComparisonFilter:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
            - eq
            - ne
        key:
          type: string
        value:
          type: string
      required:
        - type
        - key
        - value
    CompoundFilter:
      $recursiveAnchor: true
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum:
            - and
            - or
        filters:
          type: array
          items:
            oneOf:
              - $ref: "#/components/schemas/ComparisonFilter"
              - $recursiveRef: "#"
            discriminator:
              propertyName: type
      required:
        - type
        - filters
