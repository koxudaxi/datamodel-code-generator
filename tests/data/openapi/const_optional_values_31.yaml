openapi: 3.1.0
info:
  title: Const Optional Values
  version: 1.0.0
paths: {}
components:
  schemas:
    ConstOptionalValues:
      type: object
      properties:
        ratio:
          const: 1.5
        settings:
          const:
            enabled: true
            limit: 3
        choices:
          const:
            - 1
            - two
        name:
          const: fixed
      required:
        - name
