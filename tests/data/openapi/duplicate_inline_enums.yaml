openapi: 3.0.0
info:
  title: Duplicate inline enums
  version: '1.0'
paths: {}
components:
  schemas:
    FirstRecord:
      type: object
      properties:
        status:
          type: string
          enum:
            - pending
            - complete
    SecondRecord:
      type: object
      properties:
        status:
          type: string
          enum:
            - pending
            - complete
        note:
          type: string
    DifferentRecord:
      type: object
      properties:
        status:
          type: string
          enum:
            - pending
            - failed
