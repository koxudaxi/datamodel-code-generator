openapi: 3.1.0
info:
  title: Empty Dict Default Test
  version: 0.1.0
servers:
- url: http://example.com
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
components:
  schemas:
    ObjectMeta:
      type: object
      properties:
        name:
          type: string
        namespace:
          type: string
    PodSpec:
      type: object
      properties:
        metadata:
          allOf:
            - $ref: '#/components/schemas/ObjectMeta'
          default: {}
