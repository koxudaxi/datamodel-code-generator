openapi: "3.1.0"
info:
  title: No-merge inherited false schema reference
  version: "1.0"
paths: {}
components:
  schemas:
    Never: false

    Base:
      type: object
      required:
        - value
      properties:
        value:
          type: string

    Derived:
      allOf:
        - $ref: "#/components/schemas/Base"
        - type: object
          properties:
            value:
              allOf:
                - $ref: "#/components/schemas/Never"
                - minLength: 2
