openapi: "3.1.0"
info:
  title: Test allOf with parent having bool property schema
  version: "1.0.0"
components:
  schemas:
    Parent:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        allowed: true
    Child:
      allOf:
        - $ref: "#/components/schemas/Parent"
        - type: object
          properties:
            name:
              maxLength: 100
