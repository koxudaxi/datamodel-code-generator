openapi: 3.1.0
info:
  title: Empty List Default Test
  version: 0.1.0
servers:
- url: http://example.com
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
components:
  schemas:
    Container:
      type: object
      properties:
        name:
          type: string
    PodSpec:
      type: object
      properties:
        containers:
          type: array
          items:
            $ref: '#/components/schemas/Container'
          default: []
