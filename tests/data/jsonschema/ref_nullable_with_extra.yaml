$schema: "http://json-schema.org/draft-07/schema#"
definitions:
  User:
    type: object
    properties:
      name:
        type: string
type: object
properties:
  user_with_extra:
    $ref: "#/definitions/User"
    nullable: true
    if:
      properties:
        name:
          const: admin
