{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UniqueItemsTupleFiniteLimit",
  "type": "object",
  "properties": {
    "values": {
      "type": "array",
      "prefixItems": [
        {
          "enum": ["a", "b"]
        },
        {
          "enum": ["a", "b"]
        },
        {
          "enum": ["a", "b"]
        }
      ],
      "items": false,
      "uniqueItems": true
    }
  },
  "required": ["values"]
}
