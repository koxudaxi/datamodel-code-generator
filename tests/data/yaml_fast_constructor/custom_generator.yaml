defaults: &defaults
  category: base
  labels: &labels
    - default
entry: &entry
  <<: *defaults
  labels: *labels
  nested:
    - name: first
      references: *labels
copy: *entry
self: &self
  child: *self
legacy_bool: True
