openapi: 3.1.0
info:
  title: Skill Fixture API
  version: 1.0.0
paths: {}
components:
  schemas:
    SkillPet:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
        name:
          type: string
        tag:
          type: string
